{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aerial-rescue.invalid/schemas/v1/event/gateway-response.schema.json",
  "description": "The command gateway's mission-scoped record of an answer it sent: the envelope with its gateway-record type, schema, producer, and response payload bound together. The payload is the same closed body the requestor received as the raw RPC reply, but this event is published only on the distinct gateway-record family.",
  "allOf": [
    {
      "$ref": "https://aerial-rescue.invalid/schemas/v1/envelope.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "const": "aerial-rescue.v1.gateway.record"
        },
        "dataschema": {
          "const": "https://aerial-rescue.invalid/schemas/v1/payload/gateway-response.schema.json"
        },
        "data": {
          "$ref": "https://aerial-rescue.invalid/schemas/v1/payload/gateway-response.schema.json"
        }
      }
    }
  ]
}
